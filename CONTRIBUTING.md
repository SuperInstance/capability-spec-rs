# Contributing
```bash
cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check
```
1. Fork → feature branch → PR
2. All public APIs need doc comments
3. No unsafe without justification
